#include <stdio.h>
#include <stdlib.h>

#include "application.h"

int main()
{
    app_run();
    /*
    srand(1000);
    game_new();
    game_print();

    printf("\n");


    int c[4][4][2] =
    {
        {{0, 0}, {1, 0}, {2, 0}, {3, 0}},
        {{0, 1}, {1, 1}, {2, 1}, {3, 1}},
        {{0, 2}, {1, 2}, {2, 2}, {3, 2}},
        {{0, 3}, {1, 3}, {2, 3}, {3, 3}},
    };

    game_print();

    char str[2];
    do
    {
        scanf("%s", str);
        if(str[0] == 'w')
        {
            game_slide_up();
        }
        if(str[0] == 's')
        {
            game_slide_down();
        }
        if(str[0] == 'a')
        {
            game_slide_left();
        }
        if(str[0] == 'd')
        {
            game_slide_right();
        }

        game_print();

    } while(str[0] != 'x' && !game_is_game_over());

    if(game_is_game_over())
    {
        fprintf(stderr, "GAME OVER\n");
    }
    */
    return 0;
}
